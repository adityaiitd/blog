import { ExploreGuide } from "@/components/explore/ExploreGuide";

export const metadata = { title: "Where to eat, wander and slow down — Our Italy Escape" };

export default function ExplorePage() {
  return <main className="page-shell"><ExploreGuide /></main>;
}
